<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gleb.fewactivities">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondActivity" />
        <activity android:name=".TimeActivity" />
        <intent-filter>
            <action android:name="com.example.gleb.intent.action.showtime"/>
            <category android:name="gleb.intent.category.DEFAULT"/>
        </intent-filter>
        <activity android:name=".DateActivity"></activity>
        <intent-filter>
            <action android:name="com.example.gleb.intent.action.showdate"/>
            <category android:name="gleb.intent.category.DEFAULT"/>
        </intent-filter>
    </application>

</manifest>